# Hyperledger Fabric 바이너리 및 샘플 (280MB, 별도 설치)
fabric-samples/

# ── 인증서 / 개인키 (절대 커밋 금지) ──────────────────────────
crypto-config.zip
network/crypto-config/
network/channel-artifacts/
network/organizations/
**/keystore/
**/*.pem
**/*.key
**/*.priv

# ── 체인코드 패키지 / 빌드 산출물 ────────────────────────────
network/*.tar.gz
chaincode/voting/ccaas-package/

# ── vendor 내 시크릿 포함 파일 (GitHub Push Protection) ────────
chaincode/voting/vendor/github.com/go-openapi/spec/appveyor.yml

# ── 컴파일된 Go 바이너리 ───────────────────────────────────────
chaincode/voting/voting

# ── Node.js ────────────────────────────────────────────────────
node_modules/
application/node_modules/
application/.env
npm-debug.log*

# ── 환경변수 ───────────────────────────────────────────────────
.env
**/.env
**/.env.*
!.env.example
!**/.env.example
*.secret

# ── 서버 런타임 데이터(저장소 밖에 보관) ───────────────────────
.runtime/
runtime/
results/

# ── 로그 ───────────────────────────────────────────────────────
*.log

# ── Caliper 결과 리포트 (생성된 HTML/PNG) ─────────────────────
caliper/reports/*.html
caliper/reports/*.png

# ── 테스트 임시 파일 ──────────────────────────────────────────
*.csv
normal_times.txt
panic_times.txt

# ── 시크릿 키 파일 ──────────────────────────────────────────────
application/.env.keys

# ── 런타임 생성 로그 / 산출물 ───────────────────────────────────
application/audit-logs/
application/benchmark-reports/*.log
caliper/reports/*.log
frontend/dist/

# ── AI 도구 설정 / 핸드오프 ────────────────────────────────────
CLAUDE.md
HANDOFF.md
docs/CODEX_NEXT_STEPS.md
docs/server_run_guide.md
caliper/report.html

# ── OS ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# 쇼케이스 배포 산출물(자동 생성)
showcase-dist/

# 평가 원자료(로컬 유지, GitHub 미포함)
docs/performance/
docs/security-eval/

# 벤치 평가 원자료(로컬 유지, GitHub 미포함)
application/benchmark-reports/
